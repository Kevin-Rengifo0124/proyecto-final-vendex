<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:fitsSystemWindows="true"
    tools:openDrawer="start"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Vendedor.MainActivityVendedor">

    <include
        android:id="@+id/appBarMain"
        layout="@layout/app_bar_main"
        />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_gravity="start"
        app:headerLayout="@layout/nav_header_vendedor"
        app:menu="@menu/nav_drawer_vendedor_menu"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:background="@color/vendex_card_bg"
        app:itemTextColor="@color/colorBlanco"
        app:itemIconTint="@color/vendex_blue_primary"
        app:itemRippleColor="@color/vendex_input_bg"
        app:itemShapeFillColor="@color/vendex_input_bg"
        app:itemShapeAppearanceOverlay="@style/ShapeAppearance.MaterialComponents.SmallComponent"
        app:itemHorizontalPadding="12dp"
        app:itemBackground="@drawable/bg_item_menu"/>

</androidx.drawerlayout.widget.DrawerLayout>